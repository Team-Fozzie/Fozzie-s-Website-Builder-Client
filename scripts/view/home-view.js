'use strict';

let app = app || {};

(function (module) {
    const homeView = {};

    homeView.initHomeView = function () {
        $('everything else').hide();
        $('#home-view').show();
    }



})(app);