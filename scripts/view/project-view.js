'use strict';

let app = app || {};

(function (module) {
    const projectView = {};

    projectView.initProjectView = function () {
        $('everything else').hide();
        $('#project-view').show();
    }



})(app);