'use strict';

var app = app || {};

(function (module) {
    var projectView = {};

    projectView.initProjectView = function () {
        $('everything else').hide();
        $('#project-view').show();
    }



})(app);